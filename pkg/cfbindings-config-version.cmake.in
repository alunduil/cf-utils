set(PACKAGE_VERSION "@CF_UTILS_VERSION@")

if(NOT "${PACKAGE_FIND_VERSION}" VERSION_GREATER "@CF_UTILS_VERSION@")
	set(PACKAGE_VERSION_COMPATIBLE 1) # Compatible with Older
	if("${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@CF_UTILS_VERSION@")
		set(PACKAGE_VERSION_EXACT 1)
	endif()
endif()
